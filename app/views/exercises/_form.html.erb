<%= simple_form_for([@workout, @workout.exercises.new]) do |f| %>
  <%= f.input :name, class: "form-control" %>
  <%= f.input :sets, class: "form-control" %>
  <%= f.input :reps, class: "form-control" %>
  <br>
  <%= f.submit %>
<% end %>
